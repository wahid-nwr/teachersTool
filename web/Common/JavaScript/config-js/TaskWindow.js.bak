/*
Ext.air.NativeWindowManager.getTaskWindow = function(taskId){
	var win, winId = 'task' + taskId;
	if(win = this.get(winId)) {
		win.instance.orderToFront();
	} else {
		win = new Ext.air.NativeWindow({
			id: winId,
			file: 'task.html?taskId=' + taskId,
			width: 500,
			height:350
		});
	}
	return win;
}

Ext.air.NativeWindowManager.getReminderWindow = function(taskId){
	var win, winId = 'reminder' + taskId;
	if(win = this.get(winId)) {
		win.instance.orderToFront();
	} else {
		win = new Ext.air.NativeWindow({
			id: winId,
			file: 'reminder.html?taskId=' + taskId,
			width:400,
			height:140,
			maximizable: false,
			resizable: false
		});
	}
	return win;
}
*/
Ext.air.NativeWindowManager.getAboutWindow = function(){
	var win, winId = 'about';
	if(win = this.get(winId)) {
		win.instance.orderToFront();
	} else {
		win = new Ext.air.NativeWindow({
			id: winId,
			file: 'about.html',
			width:350,
			height:300,
			resizable: false,
            type:'utility'
        });
	}
	return win;
}
Ext.air.NativeWindowManager.getFormWindow = function(){
	var win, winId = 'form';
	if(win = this.get(winId)) {
		win.instance.orderToFront();
	} else {
		win = new Ext.air.NativeWindow({
			id: winId,
			file: 'dynamic.html',
			width: 509,
			height:501,
			resizable: true,
            type:'utility'
        });
	}
	return win;
}
Ext.air.NativeWindowManager.getZoneWindow = function(){
	var win, winId = 'zone';
	if(win = this.get(winId)) {
		win.instance.orderToFront();
	} else {
		win = new Ext.air.NativeWindow({
			id: winId,
			file: 'zone.html',
			width: 509,
			height:501,
			resizable: true,
            type:'utility'
        });
	}
	return win;
}
Ext.air.NativeWindowManager.getAreaWindow = function(){
	var win, winId = 'area';
	if(win = this.get(winId)) {
		win.instance.orderToFront();
	} else {
		win = new Ext.air.NativeWindow({
			id: winId,
			file: 'area.html',
			width: 800,
			height:700,
			resizable: true,
            type:'utility'
        });
	}
	return win;
}
Ext.air.NativeWindowManager.getComboWindow = function(){
	var win, winId = 'combo';
	if(win = this.get(winId)) {
		win.instance.orderToFront();
	} else {
		win = new Ext.air.NativeWindow({
			id: winId,
			file: 'index.html',
			width: 800,
			height:700,
			resizable: true,
            type:'utility'
        });
	}
	return win;
}
Ext.air.NativeWindowManager.getBranchWindow = function(){
	var win, winId = 'branch';
	if(win = this.get(winId)) {
		win.instance.orderToFront();
	} else {
		win = new Ext.air.NativeWindow({
			id: winId,
			file: 'branch.html',
			width: 509,
			height:501,
			resizable: true,
            type:'utility'
        });
	}
	return win;
}
Ext.air.NativeWindowManager.getCenterWindow = function(){
	var win, winId = 'center';
	if(win = this.get(winId)) {
		win.instance.orderToFront();
	} else {
		win = new Ext.air.NativeWindow({
			id: winId,
			file: 'center.html',
			width: 509,
			height:501,
			resizable: true,
            type:'utility'
        });
	}
	return win;
}
Ext.air.NativeWindowManager.getLoaneeWindow = function(){
	var win, winId = 'loanee';
	if(win = this.get(winId)) {
		win.instance.orderToFront();
	} else {
		win = new Ext.air.NativeWindow({
			id: winId,
			file: 'loanee.html',
			width: 509,
			height:501,
			resizable: true,
            type:'utility'
        });
	}
	return win;
}
/*
Ext.air.NativeWindowManager.getPrefWindow = function(){
	var win, winId = 'prefs';
	if(win = this.get(winId)) {
		win.instance.orderToFront();
	} else {
		win = new Ext.air.NativeWindow({
			id: winId,
			file: 'preferences.html',
			width:240,
			height:150,
			resizable: false,
            type:'utility'
        });
	}
	return win;
}
*/